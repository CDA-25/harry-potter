services:
  mytodo:
    build:
      context: .
      target: dev
      dockerfile: Dockerfile
    image: mytodo:dev
    container_name: harry-potter
    volumes:
      - ./:/app
    ports:
      - "3000:3000"