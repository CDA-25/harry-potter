services:
  harrypotter:
    build:
      context: .
      target: dev
      dockerfile: Dockerfile
    image: harrypotter:dev
    container_name: harrypotter_dev
    volumes:
      - ./:/app
      - /app/node_modules
    ports:
      - "5173:5173"